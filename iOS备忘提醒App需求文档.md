# iOS 备忘提醒 App 需求文档

## 1. 产品概述

本产品是一款面向 iPhone 用户的轻量级备忘提醒工具。用户可以快速记录一件需要完成的事情，设置提醒时间并保存。系统会在提醒时间前 30 分钟触发手机震动和提醒，并在支持灵动岛的 iPhone 设备上通过灵动岛展示提醒入口。用户点击灵动岛入口后，可以看到一个小窗口，窗口中展示之前记录的提醒内容。

示例场景：

用户输入“明天买一个笔记本电脑”，选择提醒时间为明天上午 10:00，点击保存。到明天上午 9:30 时，手机震动并出现提醒入口。用户点击灵动岛图标后，弹出小窗口，显示“明天买一个笔记本电脑”。

## 2. 产品目标

1. 让用户用尽可能少的步骤创建一条备忘提醒。
2. 在提醒时间前 30 分钟主动通知用户。
3. 借助 iPhone 的震动、通知和灵动岛能力，提高提醒的可见性。
4. 点击灵动岛后，让用户快速查看提醒内容，不需要进入复杂页面。

## 3. 目标用户

主要用户：

- 经常忘记临时事项的 iPhone 用户。
- 需要简单提醒工具，但不想使用复杂日程软件的用户。
- 使用支持灵动岛设备的用户，如 iPhone 14 Pro 及以上 Pro 系列、后续支持灵动岛的 iPhone 机型。

典型使用场景：

- 明天买东西。
- 下午给某人打电话。
- 半小时后取快递。
- 晚上提交作业或文件。
- 某个时间点之前准备会议材料。

## 4. 核心功能范围

### 4.1 创建备忘提醒

用户可以在 App 内创建一条提醒。

功能要求：

- 用户可以输入提醒内容。
- 用户可以选择具体提醒时间。
- 用户点击保存后，系统保存该提醒。
- 保存成功后，页面给出明确反馈。

字段要求：

| 字段 | 类型 | 是否必填 | 说明 |
| --- | --- | --- | --- |
| 提醒内容 | 文本 | 是 | 用户希望被提醒的事情，例如“明天买一个笔记本电脑” |
| 提醒时间 | 日期时间 | 是 | 用户选择希望被提醒的目标时间 |
| 提前提醒时间 | 固定值 | 是 | 默认在提醒时间前 30 分钟提醒 |
| 创建时间 | 日期时间 | 自动生成 | 用于记录创建时间 |
| 状态 | 枚举 | 自动生成 | 未提醒、已提醒、已完成、已取消 |

输入限制：

- 提醒内容不能为空。
- 提醒内容建议限制在 200 字以内。
- 提醒时间必须晚于当前时间。
- 如果提醒时间距离当前时间少于 30 分钟，系统需要提示用户“该提醒距离现在不足 30 分钟，将在可用时间立即提醒或按实际时间提醒”。

### 4.2 保存提醒

用户点击“保存”后，App 需要完成以下操作：

1. 校验提醒内容和提醒时间。
2. 将提醒数据保存到本地。
3. 创建本地通知任务。
4. 如果设备支持，准备灵动岛相关展示能力。
5. 告知用户保存成功。

保存失败时：

- 如果内容为空，提示“请输入提醒内容”。
- 如果时间无效，提示“请选择未来的提醒时间”。
- 如果通知权限未开启，引导用户开启通知权限。

### 4.3 提前 30 分钟提醒

系统需要在用户选择的提醒时间前 30 分钟触发提醒。

提醒方式：

- 手机震动。
- 系统通知。
- 支持灵动岛的设备上显示灵动岛入口。

规则：

- 默认提前时间为 30 分钟。
- 第一版可以先固定为 30 分钟，不提供自定义提前时间。
- 如果用户设置的目标时间距离当前时间不足 30 分钟，提醒触发时间需要根据系统能力进行兼容处理。

### 4.4 灵动岛提醒入口

在支持灵动岛的 iPhone 上，提醒触发后，灵动岛展示一个提醒图标或简短提醒状态。

展示要求：

- 灵动岛中显示一个简洁图标。
- 图标表达“提醒”或“备忘”的含义。
- 用户点击灵动岛后，进入提醒详情小窗口或对应详情页面。

兼容要求：

- 不支持灵动岛的设备，需要通过普通系统通知进入提醒详情。
- 如果 iOS 系统限制导致灵动岛展示需要 Live Activity 支持，则需要使用 ActivityKit 实现。
- App 需要处理用户未授权通知、Live Activity 不可用、设备不支持灵动岛等情况。

### 4.5 点击后展示小窗口

用户点击灵动岛入口后，App 展示一个小窗口，显示提醒内容。

小窗口内容：

- 提醒内容，例如“明天买一个笔记本电脑”。
- 原定提醒时间。
- 操作按钮：
  - “知道了”
  - “完成”
  - “稍后提醒”

第一版建议保留最小功能：

- 必须展示提醒内容。
- 必须提供关闭按钮。
- 可以提供“完成”按钮。
- “稍后提醒”可以作为第二版功能。

## 5. 页面需求

### 5.1 首页 / 创建提醒页

页面目标：

让用户进入 App 后可以立即创建提醒。

页面元素：

- 顶部标题：备忘提醒。
- 文本输入框：输入要提醒的事情。
- 日期时间选择器：选择提醒时间。
- 保存按钮。
- 最近提醒列表，展示已创建但未完成的提醒。

交互要求：

- 用户输入内容后，保存按钮可点击。
- 用户点击时间选择区域后，可以选择日期和时间。
- 点击保存后，如果校验通过，则保存提醒。
- 保存成功后清空输入框，或跳转到提醒列表。

### 5.2 提醒列表页

页面目标：

让用户查看已经创建的提醒。

列表项展示：

- 提醒内容。
- 提醒时间。
- 状态。

支持操作：

- 标记完成。
- 删除提醒。
- 编辑提醒。

第一版优先级：

- 查看提醒列表：必须。
- 删除提醒：建议。
- 编辑提醒：可放到第二版。

### 5.3 提醒详情小窗口

页面目标：

在用户点击通知或灵动岛后，快速展示提醒内容。

展示内容：

- 提醒标题或图标。
- 提醒正文。
- 提醒时间。
- 关闭或完成按钮。

视觉要求：

- 信息清晰。
- 内容居中或靠上展示。
- 不需要复杂装饰。
- 重点突出提醒文本。

## 6. 用户流程

### 6.1 创建提醒流程

1. 用户打开 App。
2. 用户输入提醒内容。
3. 用户选择提醒时间。
4. 用户点击保存。
5. App 校验输入内容。
6. App 保存提醒。
7. App 注册本地通知。
8. App 提示保存成功。

### 6.2 触发提醒流程

1. 到达提醒时间前 30 分钟。
2. 系统触发通知和震动。
3. 支持灵动岛的设备显示灵动岛入口。
4. 用户点击灵动岛或通知。
5. App 打开提醒详情小窗口。
6. 用户查看提醒内容。
7. 用户点击“完成”或关闭。

## 7. 权限需求

App 需要向用户申请以下权限：

| 权限 | 用途 | 是否必需 |
| --- | --- | --- |
| 通知权限 | 发送提醒通知 | 必需 |
| 声音 / 震动 | 提醒时震动或提示 | 必需 |
| Live Activity 权限 | 支持灵动岛展示 | 视设备和系统能力而定 |

权限说明文案建议：

“开启通知后，我们才能在提醒时间前通知你。”

## 8. 技术实现建议

### 8.1 iOS 技术方向

建议使用：

- Swift / SwiftUI 开发 App。
- UserNotifications 框架实现本地通知。
- ActivityKit 实现 Live Activity 和灵动岛展示。
- SwiftData 或 Core Data 保存提醒数据。

### 8.2 本地通知

使用 UserNotifications 创建本地通知。

通知触发时间：

提醒触发时间 = 用户选择的提醒时间 - 30 分钟。

如果计算后的触发时间早于当前时间：

- 第一版建议立即创建一个尽快触发的通知，或提示用户重新选择时间。

### 8.3 灵动岛与 Live Activity

灵动岛并不是普通 App 可以随意常驻展示的区域，通常需要通过 Live Activity 实现。第一版需要明确支持范围：

- 支持 iOS 16.1 及以上的 Live Activity。
- 支持具备灵动岛的设备展示灵动岛样式。
- 不支持的设备使用锁屏 Live Activity 或普通通知。

注意事项：

- Live Activity 适合展示正在进行或即将发生的事项。
- iOS 对 Live Activity 的持续时间和更新频率有限制。
- 如果只是一次性提醒，普通通知是最稳定的实现方式；灵动岛展示可以作为增强体验。

## 9. 数据结构建议

提醒对象 Reminder：

| 字段名 | 示例 | 说明 |
| --- | --- | --- |
| id | UUID | 唯一标识 |
| content | 明天买一个笔记本电脑 | 提醒内容 |
| targetTime | 2026-05-20 10:00 | 用户选择的目标时间 |
| notifyTime | 2026-05-20 09:30 | 实际提醒时间 |
| status | pending | 当前状态 |
| createdAt | 2026-05-19 15:30 | 创建时间 |
| updatedAt | 2026-05-19 15:30 | 更新时间 |

状态枚举：

- pending：未提醒。
- notified：已提醒。
- completed：已完成。
- cancelled：已取消。

## 10. 非功能需求

### 10.1 易用性

- 创建提醒流程不超过 3 个主要步骤。
- 用户第一次打开 App 时，应清楚知道如何创建提醒。
- 保存成功、失败、权限未开启都需要有明确反馈。

### 10.2 可靠性

- 本地提醒需要尽量稳定触发。
- App 关闭后，已注册的本地通知仍应生效。
- 用户修改或删除提醒后，对应通知也需要同步修改或取消。

### 10.3 性能

- 首页打开速度应尽量快。
- 本地提醒列表加载应流畅。
- 第一版不需要云同步，减少复杂度。

### 10.4 隐私

- 提醒内容只保存在本机。
- 第一版不上传用户数据。
- 隐私政策中需要说明提醒内容的存储方式。

## 11. 版本规划

### 11.1 MVP 第一版

必须实现：

- 创建提醒。
- 输入提醒内容。
- 选择提醒时间。
- 保存提醒。
- 提前 30 分钟本地通知。
- 手机震动。
- 点击通知进入提醒详情。
- 提醒列表。
- 删除提醒。

建议实现：

- 支持灵动岛展示提醒入口。
- 支持点击灵动岛进入提醒详情。
- 标记提醒为完成。

### 11.2 第二版

可增加：

- 自定义提前提醒时间，例如提前 5 分钟、15 分钟、30 分钟、1 小时。
- 稍后提醒。
- 重复提醒，例如每天、每周、每月。
- 分类或标签。
- 搜索提醒。
- iCloud 同步。
- 桌面小组件。

## 12. 验收标准

### 12.1 创建提醒

- 用户输入“明天买一个笔记本电脑”并选择未来时间后，点击保存，提醒创建成功。
- 保存成功后，提醒出现在列表中。
- 内容为空时不能保存，并显示错误提示。
- 选择过去时间时不能保存，并显示错误提示。

### 12.2 通知提醒

- 到达目标提醒时间前 30 分钟，手机收到通知。
- 手机根据系统设置产生震动。
- 用户点击通知后，可以看到提醒详情。
- 删除提醒后，不再收到该提醒通知。

### 12.3 灵动岛

- 在支持灵动岛和 Live Activity 的设备上，提醒触发时显示灵动岛入口。
- 用户点击灵动岛入口后，可以看到提醒内容。
- 在不支持灵动岛的设备上，普通通知仍然可用。

## 13. 风险与限制

1. iOS 对灵动岛的控制较严格，不能像普通页面元素一样任意展示图标。
2. 灵动岛通常需要通过 Live Activity 实现，且系统会限制展示时长和行为。
3. 本地通知的震动行为受用户系统设置影响，如果用户关闭震动或通知，App 无法强制震动。
4. 如果用户没有开启通知权限，提醒功能会受到影响。
5. 如果用户设备不支持灵动岛，需要使用普通通知作为主要提醒入口。

## 14. 待确认问题

1. 第一版是否必须强制支持灵动岛，还是可以先用普通通知完成核心提醒？
2. 用户点击灵动岛后，是打开 App 内详情页，还是展示类似弹窗的页面？
3. 是否需要支持重复提醒？
4. 是否需要账号登录和云同步？
5. 提醒内容是否需要支持图片、语音或附件？
6. “提前 30 分钟”是否固定，还是允许用户修改？

